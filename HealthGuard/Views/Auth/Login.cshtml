@{
    Layout = null;
    ViewData["Title"] = "Đăng nhập - HealthGuard";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        /* CSS GIỮ NGUYÊN 100% */
        body {
            background-color: #f8fafc;
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }

        .login-wrapper {
            width: 100%;
            max-width: 420px;
            padding: 20px;
        }

        .login-card {
            background: white;
            border-radius: 24px;
            padding: 40px 30px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.04);
            text-align: center;
        }

        .shield-icon {
            width: 48px;
            height: 48px;
            background: #eff6ff;
            color: #0d47a1;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin: 0 auto 20px;
        }

        .welcome-title {
            font-size: 1.25rem;
            font-weight: 800;
            color: #0d47a1;
            margin-bottom: 8px;
        }

        .welcome-subtitle {
            font-size: 0.9rem;
            color: #64748b;
            margin-bottom: 30px;
        }

        .input-group-custom {
            position: relative;
            margin-bottom: 20px;
            text-align: left;
        }

            .input-group-custom i {
                position: absolute;
                left: 16px;
                top: 50%;
                transform: translateY(-50%);
                color: #64748b;
                font-size: 1.1rem;
            }

        .form-control-custom {
            width: 100%;
            background: #f1f5f9;
            border: 1px solid transparent;
            border-radius: 16px;
            padding: 14px 16px 14px 45px;
            font-size: 0.95rem;
            color: #334155;
            transition: 0.2s;
            outline: none;
        }

            .form-control-custom:focus {
                background: white;
                border-color: #0d47a1;
                box-shadow: 0 0 0 4px rgba(13, 71, 161, 0.1);
            }

        .btn-primary-custom {
            background: #0d47a1;
            color: white;
            border: none;
            width: 100%;
            padding: 14px;
            border-radius: 16px;
            font-weight: 600;
            transition: 0.2s;
            margin-bottom: 25px;
            cursor: pointer;
        }

            .btn-primary-custom:hover {
                background: #155eef;
            }

        .divider {
            display: flex;
            align-items: center;
            text-align: center;
            margin-bottom: 25px;
            color: #94a3b8;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

            .divider::before, .divider::after {
                content: '';
                flex: 1;
                border-bottom: 1px solid #e2e8f0;
            }

            .divider:not(:empty)::before {
                margin-right: 15px;
            }

            .divider:not(:empty)::after {
                margin-left: 15px;
            }

        .btn-google {
            background: #f1f5f9;
            color: #334155;
            border: none;
            width: 100%;
            padding: 12px;
            border-radius: 16px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            transition: 0.2s;
            text-decoration: none;
        }

            .btn-google:hover {
                background: #e2e8f0;
            }

            .btn-google img {
                width: 20px;
            }

        .register-link {
            margin-top: 25px;
            font-size: 0.9rem;
            color: #64748b;
        }

            .register-link a {
                color: #0d47a1;
                font-weight: 700;
                text-decoration: none;
            }

        .guest-link {
            color: #64748b;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-top: 15px;
            text-decoration: none;
            transition: 0.2s;
            font-size: 0.85rem;
        }

            .guest-link:hover {
                color: #0d47a1;
            }
    </style>
</head>
<body>

    <div class="login-wrapper">
        <div class="login-card">
            <div class="shield-icon"><i class="bi bi-shield-plus"></i></div>
            <h1 class="welcome-title">Chào mừng trở lại</h1>
            <p class="welcome-subtitle">Đăng nhập vào tài khoản HealthGuard của bạn.</p>

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger" style="border-radius: 12px; font-size: 0.85rem; padding: 10px;">
                    <i class="bi bi-exclamation-circle me-2"></i>@ViewBag.Error
                </div>
            }

            <form asp-controller="Auth" asp-action="Login" method="post">
                <div class="text-start" style="font-size: 0.75rem; font-weight: 700; color: #64748b; margin-bottom: 8px;">TÊN ĐĂNG NHẬP HOẶC EMAIL</div>

                <div class="input-group-custom">
                    <i class="bi bi-person"></i>
                    <input type="text" name="username" class="form-control-custom" placeholder="Nhập tên đăng nhập hoặc email" required>
                </div>

                <div class="input-group-custom">
                    <i class="bi bi-lock"></i>
                    <input type="password" name="password" class="form-control-custom" placeholder="Nhập mật khẩu của bạn" required>
                </div>

                <button type="submit" class="btn-primary-custom">Đăng nhập</button>
            </form>

            <div class="divider">Hoặc tiếp tục với</div>

            <button class="btn-google">
                <img src="https://www.svgrepo.com/show/475656/google-color.svg" alt="Google">
                Đăng nhập bằng Google
            </button>

            <div class="register-link">
                Chưa có tài khoản?
                <a asp-controller="Auth" asp-action="Register">Đăng ký ngay</a>

                <a asp-controller="Home" asp-action="Index" class="guest-link">
                    <i class="bi bi-person-bounding-box"></i> Truy cập ẩn danh
                </a>
            </div>
        </div>
    </div>

</body>
</html>